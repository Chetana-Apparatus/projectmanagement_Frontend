# ------------------------------------------------------------------------------
# TEMP — integration notes (remove whole block once UI ↔ API work is finalized).
# While changing UI we still log in manually against local Django:
# run backend (`runserver`), create users in Django admin if needed, then login here.
# ------------------------------------------------------------------------------

# Loaded automatically by `next dev` (overrides same keys from `.env`).
# Local Django API — BACKEND_URL for proxy below.

BACKEND_API_ORIGIN=http://127.0.0.1:8000
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
NEXT_PUBLIC_base_url=http://127.0.0.1:8000/
