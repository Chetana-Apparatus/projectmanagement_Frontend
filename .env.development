# ------------------------------------------------------------------------------
# TEMP — integration notes (remove whole block once UI ↔ API work is finalized).
# Frontend integration points at the deployed Django backend below.
# ------------------------------------------------------------------------------

# Loaded automatically by `next dev` (overrides same keys from `.env`).
# Deployed Django API — BACKEND_URL for proxy below.

BACKEND_API_ORIGIN=http://187.127.139.247:6009
NEXT_PUBLIC_API_BASE_URL=http://187.127.139.247:6009
NEXT_PUBLIC_base_url=http://187.127.139.247:6009/
